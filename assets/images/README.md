# Images

Add the following for full SEO/social support:

- `og-image.png` — 1200×630 social preview (used by Open Graph & Twitter Cards)
- Project screenshots (optional) to replace the gradient/emoji placeholders in `index.html`
